# Local development

Install Node.js 22 and dependencies with `npm install`. Copy `.env.example` to `.env.local`. The app works in explicit demo mode without external services; connect Supabase for real auth and persistence. Run quality commands from the README before opening a pull request.
